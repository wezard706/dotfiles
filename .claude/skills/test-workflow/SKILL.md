---
name: test-workflow
description: テスト設計から実装までを統合実行する。test-design（テストケース設計）→ test-implementation（RSpec実装）の2スキルをサブエージェントで順次実行し、最後にアウトラインの全テストケースが実装されたか・テストがグリーンかを検証する。「テストワークフローを実行して」「test-workflowして」「テストを設計から実装まで一気に」「テスト設計して実装まで進めて」「テストを最後まで仕上げて」などのリクエスト時にトリガーする。単体の test-design（設計だけ）や test-implementation（実装だけ）を求められた場合はトリガーしない。
---

# テストワークフロー（test-workflow）

test-design と test-implementation の2スキルを順次実行し、テスト対象（SUT）に対して「テストケース設計 → RSpec実装 → 検証」を一気通貫で行う。各スキルはサブエージェントで実行し、最後にワークフロー全体の成果を検証する。

---

## 良い単体テストとは

このワークフローのゴールは **良い単体テストを完成させること**。設計・実装・検証の各ステップで、成果物が次の性質に反していないか常に問い直す。設計だけ・実装だけでは判断しきれない性質（壊れにくさ・速さ等）も、ワークフロー全体を通して担保する。

- **バグを見つけられる** — そもそもの目的。バグを検出できないテストは存在価値がない
- **読めば仕様が分かる** — テストコードが仕様の説明になっている
- **壊れにくい** — 実装をリファクタしても、振る舞いが変わらなければ失敗しない（誤警報を出さない）
- **失敗した原因が分かる** — 落ちたとき、どの振る舞いが壊れたか一目で分かる
- **速い・独立している** — 他テストの実行順や状態に依存せず、単体で速く回せる

各サブスキルへの指示（Step 2・Step 3）でも、この性質を満たすよう促す。

---

## 入力と出力

**入力:** テスト対象（SUT）。test-design と同じく、仕様書・実装コードのいずれか、または両方。不足する場合はユーザーにヒアリングする。

**出力:**
1. RSpec アウトライン（test-design の成果物、`./tmp/test-design/<SUT名>-cases.rb`）
2. 実行可能な RSpec テストコード（test-implementation の成果物）
3. 検証結果の要約（全 `it` 実装済みか・テストがグリーンか・発見したバグ）

---

## ワークフロー概要

```
Step 1: 入力の確認・ヒアリング
Step 2: test-design をサブエージェントで実行 → RSpec アウトライン
Step 3: test-implementation をサブエージェントで実行 → 実行可能な RSpec
Step 4: 検証
  → 不足・失敗あり & iteration < 3 → Step 5 へ
  → 問題なし or iteration = 3 → Step 6 へ
Step 5: 修正イテレーション → Step 4 へ戻る
Step 6: ユーザーへ最終報告
```

---

## Step 1: 入力の確認

テスト対象（クラス・メソッド）と、その仕様書・実装コードの所在を確認する。test-design の入力要件（仕様書 > 実装コード > ヒアリング の優先順）を満たすことを確認する。

この段階で **SUT名**（ファイル名に使う識別子。例: `account-withdraw`）を決める。

仕様が曖昧な場合は **推測せず確認する**（test-design の「原則1: 仕様を疑う」と同じ）。

---

## Step 2: テスト設計（test-design）

サブエージェントに以下を指示する。

```
あなたはテスト設計の専門家です。以下のタスクを実行してください。

1. スキル定義を読む: .claude/skills/test-design/SKILL.md と reference.md をすべて読む
2. テスト対象: [SUTのクラス・メソッドと、仕様書/実装コードの所在を貼る]
3. スキルの手順（Step 1〜Step 2）に従ってテストケースを設計する
4. 成果物（describe/context/it の RSpec アウトライン）を ./tmp/test-design/[SUT名]-cases.rb に保存する

重要: ブラックボックスとホワイトボックスの両観点を使うこと（コードがある場合）。
テスト可用性が著しく低いと判断したら、無理に設計せず中断して理由を報告すること。
```

**完了確認:** `./tmp/test-design/[SUT名]-cases.rb` に describe/context/it のアウトラインが生成され、各 `it` に技法/網羅のコメントが付いていること。

test-design が「テスト可用性が低い」として中断した場合は、ここでワークフローを止めてユーザーに報告する（実装に進まない）。

---

## Step 3: テスト実装（test-implementation）

サブエージェントに以下を指示する。

```
あなたはRSpecテスト実装の専門家です。以下のタスクを実行してください。

1. スキル定義を読む: .claude/skills/test-implementation/SKILL.md と reference.md をすべて読む
2. テスト設計書（RSpecアウトライン）を読む: ./tmp/test-design/[SUT名]-cases.rb
3. テスト対象コードを読む: [SUTの実装コードの所在]
4. スキルの手順に従い、アウトラインの各 it を実行可能な RSpec として実装する

重要:
- アウトラインの全 it を実装すること（pending を残さない）
- アサーションは振る舞いベース（外から見える出力・状態）に保つこと
- モックは最小限に留めること
- spec ファイルの配置は test-implementation スキルの規約に従うこと
```

**完了確認:** アウトラインの全 `it` が実装され、`pending`/`skip` が残っていないこと。

---

## Step 4: 検証

実装された spec を2軸で検証する。

### 静的検証: 網羅の一致

`./tmp/test-design/[SUT名]-cases.rb` のアウトラインと、実装された spec を突き合わせる。

- アウトラインの全 `context`/`it` が実装に存在するか
- 設計になかった余計なテストが紛れ込んでいないか
- `pending`/`skip`/空ボディの `it` が残っていないか

### 動的検証: テストの実行

可能なら実際にテストを実行する: `bundle exec rspec <spec ファイル>`

- **全グリーン** → 問題なし。Step 6 へ
- **失敗あり** → 原因を切り分ける（重要）:
  - **テスト側のバグ**（前提・入力・期待値の誤り）→ Step 5 で修正する
  - **プロダクトコード側のバグ**（テストが正しく、実装が仕様に反している）→ **プロダクトコードを勝手に直さない**。これはテストが目的どおりバグを発見した状態。Step 6 でユーザーに報告する

テスト実行環境が無い（依存・DB等が用意できない）場合は、静的検証のみとし、その旨を報告する。

---

## Step 5: 修正イテレーション

検証で見つかった **テスト側の不足・誤り** を修正する。

- アウトラインに対する実装漏れ・`pending` 残り → test-implementation を再実行（不足分を指示に明記）
- 期待値・前提の誤り → 該当 `it` を修正

修正後、Step 4（検証）に戻る。**上限3イテレーション**。上限到達時は残課題をユーザーに提示する。

プロダクトコードのバグが疑われるケースはこのループで直さない。Step 6 で報告する。

---

## Step 6: ユーザーへ最終報告

以下を簡潔に報告する。

- 生成物: アウトライン（`./tmp/test-design/[SUT名]-cases.rb`）と spec ファイルのパス
- 検証結果: 全 `it` 実装済みか / テスト実行結果（グリーン・赤・未実行）
- **発見したバグ**: プロダクトコード側のバグが疑われる場合は、どの `it` が失敗し、仕様とのどの差異かを明示する
- 残課題: 上限到達で未解決の項目、テスト未実行の理由など

中間成果物（サブエージェントのログ等）はユーザーに提示しない。

---

## やってはいけないこと

- 単体の設計だけ・実装だけの依頼でこのワークフローを起動しない: それぞれ test-design / test-implementation を直接使う。
- テストを通すためにプロダクトコードを勝手に書き換えない: テストが見つけたバグは報告する。テストの目的はバグを見つけること。
- test-design が中断したのに実装へ進まない: テスト可用性の問題はユーザーに委ねる。
